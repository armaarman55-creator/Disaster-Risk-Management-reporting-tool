// Local dev config — copy this file to config.local.js and fill in your values.
// config.local.js is gitignored. This example file IS committed (no real keys).

window.DRMSA_CONFIG = {
  SUPABASE_URL: '',       // e.g. https://abcxyz.supabase.co
  SUPABASE_ANON_KEY: '',  // your anon/public key
};
