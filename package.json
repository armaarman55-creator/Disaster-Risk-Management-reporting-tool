{
  "name": "drmsa-contingency-module",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "lint": "node --check js/*.js && node --check tests/*.test.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
